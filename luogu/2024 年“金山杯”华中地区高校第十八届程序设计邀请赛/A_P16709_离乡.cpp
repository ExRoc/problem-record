#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int theta;
    cin >> theta;
    if (theta < 240) {
        cout << 3 << '\n';
    } else {
        cout << 2 << '\n';
    }

    return 0;
}